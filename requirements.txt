# PDF RAG Assistant — Python dependencies
# Install: pip install -r requirements.txt

streamlit>=1.35.0
openai>=1.30.0
pypdf>=4.2.0
chromadb>=0.5.0

# ── v2 additions ──
sentence-transformers>=2.7.0   # local embeddings for Groq mode (CPU, no quota)
